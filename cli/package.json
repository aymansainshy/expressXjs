{
  "name": "@expressxjs/cli",
  "version": "0.0.5",
  "main": "./dist/cli.js",
  "types": "./dist/cli.d.ts",
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "npm run build && node --test test/*.test.cjs",
    "build": "rm -rf dist && tsc && chmod +x dist/cli.js",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "prepublishOnly": "npm run build"
  },
  "bin": {
    "expressx": "dist/cli.js"
  },
  "author": "Ayman Abdulrahman",
  "license": "MIT",
  "description": "A CLI tool for ExpressXjs framework",
  "dependencies": {
    "@expressxjs/core": "0.0.5",
    "@types/node": "^20.0.0",
    "chokidar": "^4.0.3",
    "commander": "^11.1.0",
    "typescript": "^5.4.0"
  }
}