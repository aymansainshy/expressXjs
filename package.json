{
  "name": "expressxjs",
  "private": true,
  "workspaces": [
    "core",
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspace=@expressx/core && npm run build --workspace=@expressx/cli",
    "build:core": "npm run build --workspace=@expressx/core",
    "build:cli": "npm run build --workspace=@expressx/cli"
  }
}