{
  "name": "examples",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "expressx": {
    "sourceDir": "src",
    "outDir": "dist",
    "main": "src/index.ts"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "expressx start",
    "build": "rm -rf dist && expressx build --verbose && tsc",
    "build3": "expressx build --output build --verbose && tsc",
    "dev2": "NODE_ENV=development port=5000 nodemon src/index.ts",
    "start": "node dist/index.js",
    "build2": "tsc"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@expressxjs/cli": "*",
    "@expressxjs/core": "*"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "jest": "^30.2.0",
    "nodemon": "^3.1.4",
    "ts-jest": "^29.4.6",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.5.2"
  }
}